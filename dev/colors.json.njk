{
  {% for color, info in colors %}
    {% if info | length > 2 %}
      {{ color }}: {
        {% for shade, info in info %}
          "{{ shade }}": "{{ info.value }}",
        {% endfor %}
      },
    {% else %}
      "{{ color }}": "{{ info.value }}",
    {% endif %}
  {% endfor %}
}